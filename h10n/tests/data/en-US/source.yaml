__helpers__: |
    from h10n.tests.helpers import pluralize
    
message:
    defaults:
        count: 1
    filter: |
        $plural_form = pluralize($count)
    key: "_{plural_form}"
    msg:
        _0: "{count} item"
        _1: "{count} items"